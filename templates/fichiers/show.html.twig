{% extends 'base.html.twig' %}

{% block title %}Fichiers{% endblock %}

{% block body %}
    <h1>Fichiers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fichier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fichier.nom }}</td>
            </tr>
            <tr>
                <th>Continue_fichier</th>
                <td>{{ fichier.continueFichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fichiers_index') }}">back to list</a>

    <a href="{{ path('fichiers_edit', {'id': fichier.id}) }}">edit</a>

    {{ include('fichiers/_delete_form.html.twig') }}
{% endblock %}
